<%= form_for(@porada) do |f| %>
  <% if @porada.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@porada.errors.count, "error") %> prohibited this porada from being saved:</h2>

      <ul>
      <% @porada.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :datum %><br>
    <%= f.date_select :datum %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
