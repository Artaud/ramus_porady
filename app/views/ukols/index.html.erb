<h1>Listing ukols</h1>

<table>
  <thead>
    <tr>
      <th>Funkce</th>
      <th>Porada</th>
      <th>Splneno</th>
      <th>Deadline</th>
      <th>Obsah</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @ukols.each do |ukol| %>
      <tr>
        <td><%= ukol.funkce_id %></td>
        <td><%= ukol.porada_id %></td>
        <td><%= ukol.splneno %></td>
        <td><%= ukol.deadline %></td>
        <td><%= ukol.obsah %></td>
        <td><%= link_to 'Show', ukol %></td>
        <td><%= link_to 'Edit', edit_ukol_path(ukol) %></td>
        <td><%= link_to 'Destroy', ukol, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ukol', new_ukol_path %>
