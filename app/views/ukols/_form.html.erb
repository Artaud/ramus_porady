<%= form_for(@ukol) do |f| %>
  <% if @ukol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ukol.errors.count, "error") %> prohibited this ukol from being saved:</h2>

      <ul>
      <% @ukol.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :funkce_id %><br>
    <%= f.number_field :funkce_id %>
  </div>
  <div class="field">
    <%= f.label :porada_id %><br>
    <%= f.number_field :porada_id %>
  </div>
  <div class="field">
    <%= f.label :splneno %><br>
    <%= f.check_box :splneno %>
  </div>
  <div class="field">
    <%= f.label :deadline %><br>
    <%= f.date_select :deadline %>
  </div>
  <div class="field">
    <%= f.label :obsah %><br>
    <%= f.text_area :obsah %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
