<%= form_for(@report) do |f| %>
  <% if @report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
      <% @report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>




  <div class="field">
    <%= f.select(:funkce_id, Funkce.all.collect {|p| [p.nazev_funkce, p.id]}) %>
  </div>
  <div class="field">
    <%= f.hidden_field :porada_id, :value => @porada_id %>
  </div>
  <div class="field">
    <%= f.label :obsah %><br>
    <%= f.text_area :obsah %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
